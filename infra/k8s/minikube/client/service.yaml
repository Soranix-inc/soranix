apiVersion: v1
kind: Service
metadata:
  name: client-service
  namespace: soranix
  labels:
    app: client
    tier: frontend
spec:
  selector:
    app: client
    tier: frontend
  ports:
    - name: http
      port: 80
      targetPort: 3000
      protocol: TCP
  type: ClusterIP





