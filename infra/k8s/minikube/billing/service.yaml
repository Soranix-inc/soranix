apiVersion: v1
kind: Service
metadata:
  name: billing-service
  namespace: soranix
  labels:
    app: billing
    tier: backend
spec:
  selector:
    app: billing
    tier: backend
  ports:
    - name: http
      port: 80
      targetPort: 5011
      protocol: TCP
  type: ClusterIP





