apiVersion: v1
kind: Service
metadata:
  name: web-service
  namespace: soranix
  labels:
    app: web
    tier: frontend
spec:
  selector:
    app: web
    tier: frontend
  ports:
    - name: http
      port: 80
      targetPort: 5173
      protocol: TCP
  type: ClusterIP





