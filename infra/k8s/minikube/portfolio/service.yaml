apiVersion: v1
kind: Service
metadata:
  name: portfolio-service
  namespace: soranix
  labels:
    app: portfolio
    tier: backend
spec:
  selector:
    app: portfolio
    tier: backend
  ports:
    - name: http
      port: 80
      targetPort: 5010
      protocol: TCP
  type: ClusterIP





