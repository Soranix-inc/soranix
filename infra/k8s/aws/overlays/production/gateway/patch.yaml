apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: soranix-gateway
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  listeners:
    - name: https
      tls:
        certificateRefs:
          - name: soranix-prod-tls

