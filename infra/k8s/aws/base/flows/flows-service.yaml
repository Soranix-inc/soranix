---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: flows
  namespace: soranix
  labels:
    app: flows
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: flows
      tier: backend
  template:
    metadata:
      labels:
        app: flows
        tier: backend
    spec:
      serviceAccountName: soranix-sa
      containers:
        - name: flows
          image: soranix/flows:latest
          ports:
            - containerPort: 5009
              name: http
          resources:
            requests:
              cpu: '200m'
              memory: '256Mi'
            limits:
              cpu: '1000m'
              memory: '1Gi'
          livenessProbe:
            httpGet:
              path: /health
              port: 5009
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 5009
            initialDelaySeconds: 5
            periodSeconds: 5
          env:
            - name: NODE_ENV
              value: 'production'
            - name: PORT
              value: '5009'
---
apiVersion: v1
kind: Service
metadata:
  name: flows-service
  namespace: soranix
  labels:
    app: flows
    tier: backend
spec:
  selector:
    app: flows
    tier: backend
  ports:
    - name: http
      port: 80
      targetPort: 5009
      protocol: TCP
  type: ClusterIP
