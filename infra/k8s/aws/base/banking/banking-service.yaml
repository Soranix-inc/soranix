---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: banking
  namespace: soranix
  labels:
    app: banking
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: banking
      tier: backend
  template:
    metadata:
      labels:
        app: banking
        tier: backend
    spec:
      serviceAccountName: soranix-sa
      containers:
        - name: banking
          image: soranix/banking:latest
          ports:
            - containerPort: 5002
              name: http
          resources:
            requests:
              cpu: '200m'
              memory: '256Mi'
            limits:
              cpu: '1000m'
              memory: '1Gi'
          livenessProbe:
            httpGet:
              path: /health
              port: 5002
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 5002
            initialDelaySeconds: 5
            periodSeconds: 5
          env:
            - name: NODE_ENV
              value: 'production'
            - name: PORT
              value: '5002'
---
apiVersion: v1
kind: Service
metadata:
  name: banking-service
  namespace: soranix
  labels:
    app: banking
    tier: backend
spec:
  selector:
    app: banking
    tier: backend
  ports:
    - name: http
      port: 80
      targetPort: 5002
      protocol: TCP
  type: ClusterIP
