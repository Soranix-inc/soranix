---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: billing
  namespace: soranix
  labels:
    app: billing
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: billing
      tier: backend
  template:
    metadata:
      labels:
        app: billing
        tier: backend
    spec:
      serviceAccountName: soranix-sa
      containers:
        - name: billing
          image: soranix/billing:latest
          ports:
            - containerPort: 5011
              name: http
          resources:
            requests:
              cpu: '200m'
              memory: '256Mi'
            limits:
              cpu: '1000m'
              memory: '1Gi'
          livenessProbe:
            httpGet:
              path: /health
              port: 5011
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 5011
            initialDelaySeconds: 5
            periodSeconds: 5
          env:
            - name: NODE_ENV
              value: 'production'
            - name: PORT
              value: '5011'
---
apiVersion: v1
kind: Service
metadata:
  name: billing-service
  namespace: soranix
  labels:
    app: billing
    tier: backend
spec:
  selector:
    app: billing
    tier: backend
  ports:
    - name: http
      port: 80
      targetPort: 5011
      protocol: TCP
  type: ClusterIP
