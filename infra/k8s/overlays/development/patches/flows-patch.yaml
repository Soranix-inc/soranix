apiVersion: apps/v1
kind: Deployment
metadata:
  name: flows
spec:
  replicas: 1
  template:
    spec:
      containers:
        - name: flows
          resources:
            requests:
              cpu: '100m'
              memory: '128Mi'
            limits:
              cpu: '500m'
              memory: '512Mi'
          env:
            - name: NODE_ENV
              value: 'development'
            - name: PORT
              value: '5009'
