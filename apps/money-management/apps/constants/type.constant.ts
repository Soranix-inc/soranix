export type CONSTANTS = {
  NODE_ENV: 'development' | 'production';
  ENV_FILE: string;
  LOG_PATH: string;
  PORT: number;
  DATABASE_URL: string;
  REDIS_URL: string;
  ALLOWED_ORIGINS: string;
  JSON_LIMIT: string;
};
